import Input from "./components/Input";
import { AppContainer, ContentContainer } from "./styledComponent.ts";

function App() {
  return (
    <AppContainer>
      <ContentContainer>
        <Input />
      </ContentContainer>
    </AppContainer>
  );
}

export default App;
